{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages -d dist --nojekyll",
    "publish": "npm run generate && npm run deploy",
    "pregenerate": "node ./scripts/generate-manifest.js",
    "prebuild": "node ./scripts/generate-manifest.js",
    "open": "open https://michaelpaulukonis.github.io/davinci-landing"
  },
  "dependencies": {
    "@nuxt/content": "^3.4.0",
    "@nuxt/eslint": "^1.3.0",
    "@nuxt/fonts": "^0.11.1",
    "@nuxt/image": "^1.10.0",
    "eslint": "^9.25.0",
    "nuxt": "^3.16.2",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.13.2",
    "gh-pages": "^6.3.0"
  }
}
